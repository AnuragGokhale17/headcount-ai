services:
  redis:
    image: redis:7-alpine
    container_name: headcount_redis
    ports:
      - "6379:6379"
    command: redis-server --bind 0.0.0.0 --protected-mode no
    restart: unless-stopped

  deepstream:
    image: nvcr.io/nvidia/deepstream:9.0-triton-multiarch
    container_name: headcount_deepstream
    ipc: host
    ports:
      - "8010:8010"
      # REMOVED 8554 from here to avoid conflict with mediamtx
      - "8555:8555"
    privileged: true
    depends_on:
      - redis
      - mediamtx
    environment:
      - DISPLAY=$DISPLAY
      - REDIS_HOST=redis
      - NVIDIA_DRIVER_CAPABILITIES=all
      - NVIDIA_VISIBLE_DEVICES=all
    volumes:
      - ../:/workspace
      - /tmp/.X11-unix:/tmp/.X11-unix
    working_dir: /workspace/deepstream
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    command: bash /workspace/deepstream/start_ds.sh
    restart: unless-stopped

  mediamtx:
    image: bluenviron/mediamtx:latest
    container_name: mediamtx_webrtc_bridge
    ports:
      - "8889:8889"       # WebRTC HTTP
      - "8554:8554"       # RTSP Push
      # Standard WebRTC UDP ports (MediaMTX uses these by default)
      - "8000:8000/udp"
      - "8001:8001/udp"
    volumes:
      - ./mediamtx.yml:/mediamtx.yml
    restart: unless-stopped